# 📁 هيكل deployment-ready

## الهيكل الكامل

```
deployment-ready/
│
├── 📂 config/                                    # ملفات الإنتاج والإعدادات
│   ├── next.config.js                          # إعدادات Next.js المحسّنة
│   └── manifest.json                           # PWA Manifest
│
├── 📂 lib/                                       # مكتبات مساعدة
│   └── posthog-client.ts                       # عميل PostHog Analytics
│
├── 📂 hooks/                                     # React Hooks المخصصة
│   └── useAnalytics.ts                         # Hook لتتبع الأحداث
│
├── 📂 components/                               # مكونات React (للتوسع)
│
├── 📂 tests/                                     # اختبارات E2E
│   └── faq.spec.ts                             # اختبار صفحة FAQ
│
├── 📂 docs/                                      # الوثائق والمواصفات
│   ├── Content-Specification-ask.seba.md       # مواصفات المحتوى
│   ├── Acceptance-Criteria-ask.seba-Pages.md   # معايير القبول
│   └── Cursor-Prompt-ask.seba-Pages-Implementation.md  # تعليمات التنفيذ
│
├── 📂 content/                                   # ملفات البيانات
│   └── content.json                            # محتوى الموقع (معدّل)
│
├── 📄 README-deploy.md                         # دليل النشر الشامل
├── 📄 STRUCTURE.md                             # هذا الملف
└── 📄 package-updates.json                     # Dependencies الجديدة

```

---

## 📋 وصف الملفات

### config/

#### next.config.js
- **الغرض**: إعدادات Next.js للإنتاج
- **المحتوى**:
  - تحسين الصور (WebP, AVIF)
  - رؤوس الأمان (Security Headers)
  - تحسين CSS و JavaScript
  - دعم Standalone Output
- **الموقع في المشروع**: جذر المشروع

#### manifest.json
- **الغرض**: ملف PWA Manifest
- **المحتوى**:
  - معلومات التطبيق
  - الألوان والأيقونات
  - إعدادات العرض
- **الموقع في المشروع**: `public/manifest.json`

---

### lib/

#### posthog-client.ts
- **الغرض**: عميل PostHog Analytics
- **الوظائف**:
  - `initPostHog()` - تهيئة PostHog
  - `pageView()` - تتبع عرض الصفحات
  - `trackEvent()` - تتبع الأحداث المخصصة
- **الموقع في المشروع**: `lib/posthog-client.ts`
- **الاستخدام**:
  ```typescript
  import { initPostHog, pageView, trackEvent } from '@/lib/posthog-client'
  ```

---

### hooks/

#### useAnalytics.ts
- **الغرض**: React Hook لتتبع الأحداث
- **الوظائف**:
  - `usePageView()` - تتبع عرض الصفحة
  - `useTrackEvent()` - تتبع حدث مخصص
- **الموقع في المشروع**: `hooks/useAnalytics.ts`
- **الاستخدام**:
  ```typescript
  import { usePageView, useTrackEvent } from '@/hooks/useAnalytics'
  ```

---

### tests/

#### faq.spec.ts
- **الغرض**: اختبار E2E لصفحة FAQ
- **الاختبارات**:
  - تحميل الصفحة
  - فتح Accordion
  - Single Expand فقط
- **الموقع في المشروع**: `tests/faq.spec.ts`
- **التشغيل**:
  ```bash
  npm run test:e2e
  ```

---

### docs/

#### Content-Specification-ask.seba.md
- **الغرض**: مواصفات المحتوى الكاملة
- **المحتوى**:
  - بيانات الصفحات الثلاث
  - الأسئلة والإجابات
  - الإحصائيات والشهادات
- **الحالة**: معدّل (تم استبدال "50,000+" بـ "الاف العطور العالمية")

#### Acceptance-Criteria-ask.seba-Pages.md
- **الغرض**: معايير القبول والاختبار
- **المحتوى**:
  - قائمة التحقق لكل صفحة
  - متطلبات الأداء
  - معايير الأمان والإمكانية
- **الحالة**: معدّل

#### Cursor-Prompt-ask.seba-Pages-Implementation.md
- **الغرض**: تعليمات التنفيذ التفصيلية
- **المحتوى**:
  - خطوات البناء
  - متطلبات البيانات
  - معايير الجودة
- **الحالة**: معدّل

---

### content/

#### content.json
- **الغرض**: محتوى الموقع بصيغة JSON
- **المحتوى**:
  - بيانات صفحة About
  - بيانات صفحة FAQ
  - بيانات صفحة Privacy
- **الحالة**: معدّل (تم استبدال "50,000+" بـ "الاف العطور العالمية")
- **الموقع في المشروع**: `data/content.json` أو `public/content.json`

---

## 🔧 كيفية الاستخدام

### 1. النسخ
```bash
cp -r deployment-ready/* /path/to/your/project/
```

### 2. التثبيت
```bash
npm i posthog-js @playwright/test
```

### 3. التحقق
```bash
npm run build
npm run lint
npm run test:e2e
```

### 4. النشر
```bash
npm run build
vercel deploy --prod
```

---

## ✅ قائمة التحقق

- [ ] تم نسخ جميع الملفات
- [ ] تم وضع الملفات في المجلدات الصحيحة
- [ ] تم تثبيت جميع Dependencies
- [ ] تم تحديث `next.config.js`
- [ ] تم إضافة `manifest.json` إلى `public/`
- [ ] تم إضافة `posthog-client.ts` إلى `lib/`
- [ ] تم إضافة `useAnalytics.ts` إلى `hooks/`
- [ ] تم إضافة `faq.spec.ts` إلى `tests/`
- [ ] تم تحديث `content.json`
- [ ] تم تشغيل `npm run build` بنجاح
- [ ] تم تشغيل `npm run lint` بنجاح
- [ ] تم تشغيل `npm run test:e2e` بنجاح

---

## 📊 الإحصائيات

| المقياس | العدد |
|--------|-------|
| الملفات | 10 |
| المجلدات | 7 |
| سطور الكود | ~500 |
| حجم المشروع | ~150 KB |

---

## 🚀 الخطوات التالية

1. انسخ المجلد إلى مشروعك
2. اتبع التعليمات في `README-deploy.md`
3. اختبر محلياً
4. نشّر على الإنتاج

---

**آخر تحديث**: 13 يناير 2026
**الحالة**: ✅ جاهز للإنتاج
